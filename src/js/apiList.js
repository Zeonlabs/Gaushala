const apiList = {
  // Rough: {
  //   addRough: { method : 'POST', url:'rough'},
  //   // loadRough:{ method : 'get', url : 'rough'}
  // }
  income: { method: "POST", url: "income/add" }
};

// export const addQueryID = (url, id) => `${url}/${id}`;

export default apiList;
